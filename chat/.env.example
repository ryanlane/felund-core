# felundchat Python TUI — environment variables
# Copy to .env and source it before running, or export directly:
#
#   source chat/.env          # bash/zsh
#   export $(cat chat/.env | xargs)
#
# Or set permanently in your shell profile (~/.bashrc, ~/.zshrc):
#   export FELUND_API_BASE=http://your-relay-server/api

# URL of the relay/rendezvous API server.
# Leave blank to run fully peer-to-peer with no relay.
# Can also be changed at runtime via F3 (Settings) in the TUI.
FELUND_API_BASE=
