# felundchat web client — environment variables
# Copy to .env (git-ignored) and fill in your values.
#
# In development:  vite reads .env automatically
# In production:   set these variables in your hosting environment
#                  (Netlify, Vercel, Cloudflare Pages, etc.)

# URL of the relay/rendezvous API server.
# Leave blank to start with relay disabled; users can configure it in Settings (F1).
VITE_FELUND_API_BASE=
