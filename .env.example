# felund-core environment variables
# Copy this file to .env and fill in your values.
# .env is git-ignored and never committed.

# ── Relay / rendezvous server ──────────────────────────────────────────────────
# URL of the relay API server.
# Used by: Python TUI (FELUND_API_BASE) and test_relay.py
# Leave blank to run fully peer-to-peer with no relay.
FELUND_API_BASE=

# ── Web client (Vite) ─────────────────────────────────────────────────────────
# Same URL but prefixed with VITE_ so Vite exposes it to the browser bundle.
# Used by: chat-webclient (built with `npm run build`)
# Leave blank to start with relay disabled (users can set it in Settings).
VITE_FELUND_API_BASE=
