[tool.ruff]
target-version = "py39"
line-length = 100

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B", "C4", "SIM"]
ignore = [
  "E501", # handled by formatter
]

[tool.ruff.lint.isort]
known-first-party = ["felundchat"]

[tool.ruff.format]
indent-style = "space"